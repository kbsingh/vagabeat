A set of scripts that will bring up a CentOS-7 AtomicApp vagrant box and run through the examples/ to ensure they all still work fine.

This set of scripts will attempt a VirtualBox provider for Vagrant
